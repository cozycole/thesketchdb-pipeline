# Testing

```bash
docker compose up -d
docker compose exec -it orchestrator bash
root#/app python orchestrator
```

From here you're able to execute python scripts interactively (either service or one off video process)
